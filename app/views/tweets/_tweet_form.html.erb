<%= form_for @tweet do |t| %>

  <% if @tweet.errors.any? %>

    <div>
    	<!-- id = "error_explanation" -->
      <h2><%= pluralize(@tweet.errors.count, "error") %> prohibited this tweet from being posted:</h2>

      <ul>
      <% @tweet.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
 <% end %>

<div>Tweet: <%= t.text_area :content %></div>

<div><%= t.submit %></div>

<% end %>