<header class="navbar navbar-fixed-top navbar-inverse">
  <div class="navbar-inner">
    <div class="container">
      <%= link_to "FAKE TWITTER", root_path, id: "logo" %>
      <nav>
        <ul class="nav pull-right">
          
          <% if signed_in? %>
            <li>
                  <%= link_to "Sign out", sign_out_path %>
            </li>
          <% else %>
            <li>
              <%= link_to "Sign in", sign_in_path %>
            </li>
          <% end %>
        </ul>
      </nav>
    </div>
  </div>
</header>